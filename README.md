`locationlog` is useful for logging GPS and compass
data at set intervals. It uses 
[bbUI.js](http://github.com/blackberry/bbUI.js), and also uses
the `blackberry.sensors` object and its events to pull compass data.
Other than that, it's not at all tied to the BB10 ;-).
